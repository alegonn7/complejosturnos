# Sistema de Gestión de Turnos - Skeleton
Estructura base con TypeScript + Express + Prisma.

Pasos:
1. Copiar .env.example -> .env y ajustar DATABASE_URL
2. npm install
3. npx prisma generate
4. npx prisma migrate dev --name init
5. npm run dev
